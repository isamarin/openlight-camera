# Заводская калибровка: что лежит в разделе `/lightcal`

Статус: **находки, 17.08.2026**. Снято read-only с двух камер, разбор протобуфов ещё не сделан.

Калибровка живёт **в отдельном разделе**, смонтированном как `/lightcal`, и читается без рута
(`rw-rw-rw-` для всех). В `/sdcard/DCIM/Camera/lightcal/` она появляется только как **копия** —
её кладёт туда галерея, поэтому на нетронутой камере там лежит один `metadata.xml`.

## Две камеры оказались разными

| Файл | LFCLHMB7C0700105 (2018-01-08) | LFCLHMB7A1700334 (2017-10-31) |
| --- | --- | --- |
| `calibration.lri` | 333 048 Б | 333 048 Б, **другая контрольная сумма** |
| `hotpixel.rec` | 31 020 168 Б | 29 101 039 Б |
| `zoom_calib_v0.lri`, `uuid.txt` | есть | есть |
| `asic_calib_v1.lri` | есть | **нет** |
| `colorcalibmirrorhall.json` | — | **есть** |
| `colorshopfloor.txt`, `crosstalkshopfloor.txt` | — | **есть** |
| `colorcamparams.rec`, `crosstalkcamparams.rec` | — | **есть** |
| `flash.json`, `fcat2_internal_analysis.txt` | — | **есть** |

Ранний экземпляр уехал с завода с **сырыми данными производственной линии** на борту; в поздних
их вычистили, оставив только собранные `.lri`. Обратное тоже верно: на позднем появился
`asic_calib_v1.lri`, которого на раннем нет вовсе — калибровка самого ASIC, судя по имени,
въехала в процесс между октябрём 2017 и январём 2018. То есть более старая камера ценнее как источник:
она рассказывает, как калибровку делали, а не только чем она кончилась.

Одинаковый размер `calibration.lri` при разных суммах означает, что формат фиксированный, а
содержимое поштучное — то, что и ожидается от заводской калибровки.

## Зеркала: восемь модулей, и известно каких

`colorcalibmirrorhall.json` (30.10.2017) — калибровка датчиков Холла для подвижных зеркал.
На модуль записаны код Холла и две точки соответствия «показание → угол».

| Модуль | hall_code | внутрь | наружу |
| --- | --- | --- | --- |
| B1 | 810 | 856 → 45.072° | 371 → 35.642° |
| B2 | 774 | 794 → 44.694° | 342 → 35.557° |
| B3 | 821 | 818 → 44.797° | 348 → 35.429° |
| B5 | 929 | 911 → 44.735° | 475 → 35.715° |
| C1 | 479 | 567 → 46.450° | 190 → 39.933° |
| C2 | 460 | 641 → 46.884° | 234 → 40.171° |
| C3 | 532 | 731 → 46.645° | 308 → 40.027° |
| C4 | 744 | 785 → 46.649° | 365 → 39.731° |

Отсюда сразу три вещи, которых мы не знали:

- **Зеркала стоят ровно на восьми модулях** — B1, B2, B3, B5 и C1, C2, C3, C4.
- **B4 и C5 неподвижны**, и это те самые модули, которые в исследованиях Grok названы опорами
  оптического зума (цепочка A1 → B4 → C5). Опора не двигается — логично.
- **Группа A зеркал не имеет вовсе**, и **C6, монохромный телемодуль, тоже неподвижен.**

Диапазоны различаются по группам: у B зеркало ходит примерно 35.5°…44.8°, у C — 39.9°…46.7°.
Разброс между экземплярами модулей внутри группы — доли градуса, то есть калибровка нужна именно
поштучная.

**Это прямой вход в задачу позы зеркала**, которая в openfusion числится нерешённой. Две точки на
модуль дают линейное отображение кода Холла в угол; всё, что нужно сверх этого, — знать, какой код
стоял в момент съёмки (он должен быть в `.lri`: в `CameraModule` есть поле `mirror_position`).

## Вспышка: два светодиода разной температуры

`flash.json` — заводской замер, 31.10.2017:

| | CCT | освещённость | ток |
| --- | --- | --- | --- |
| CoolWhite | 5285 K | 342.3 лк | 250 мА |
| WarmWhite | 2194 K | 284.9 лк | 250 мА |

То есть вспышка **двухцветная**, и её можно смешивать под температуру сцены. Замер поштучный,
значит смешение задумывалось точным. В приложении сейчас используется только `FLASH_MODE`.

## Заводские протоколы

`colorshopfloor.txt` — построчный протокол цветовой калибровки: по каждому модулю три
осветителя (D65, A, TL84), для каждого **deltaE** и отношения каналов RG/BG, с вердиктом.
Пример: `A1_color^D65_deltaE ... deltaE: 1.203083` и `RG ratio: 0.496639, BG ratio: 0.643628`.

Это те же величины, что мы видим в `.lri` через `lri-mono --census`, — но здесь видно **как их
мерили и по какому критерию принимали**.

`crosstalkshopfloor.txt` — калибровка перекрёстных наводок: на модуль пишутся минимальное и
максимальное значения сетки с допусками (min ≥ 50, max ≤ 900). Сама сетка — в
`crosstalkcamparams.rec` (263 КБ).

## Геометрия читается: `lri-mono --calib`

Заводская геометрия лежит в `calibration.lri` протобуфом и теперь разбирается одной командой —
она же работает на любом снимке, потому что тот же блок едет в каждом кадре.

Что внутри на каждый модуль: тип зеркала, пучки калибровки по дистанциям фокусировки (818, 1500,
818 мм), интринсики с матрицей K, дисторсия, а для подвижных зеркал — полная модель:

```text
B1  mirror MOVABLE  focus bundles 3
    intrinsics       fx 8293.3  fy 8293.3  cx 2060.7  cy 1557.3  + distortion
    mirror angles    36.81° … 46.45°
    hall codes       0 … 1023        пар «код → угол»: 5
    rotation axis    (0.6032, 0.7976, -0.0079)
    mirror normal    (0.8048, -0.5935, 0.0079)
    quadratic model  0 0 1 0.001684 -0.999981 -0.001347
    reprojection     0.2474
```

То есть ось вращения, нормаль зеркала в нуле, диапазон углов, отображение кода Холла в угол
(пять опорных пар плюс квадратичная модель) и ошибка перепроецирования. Для позы зеркала этого
достаточно; остаётся взять `mirror_position` из кадра.

### Зеркала трёх типов, а не двух

| Группа | Модули | Тип |
| --- | --- | --- |
| A (28 мм) | A1–A5 | `NONE` — зеркала нет |
| B (70 мм) | B1, B2, B3, B5 | `MOVABLE` |
| B | **B4** | `GLUED` — зеркало есть, но приклеено |
| C (150 мм) | C1, C2, C3, C4 | `MOVABLE` |
| C | **C5, C6** | `GLUED` |

Прежняя запись «B4, C5 и C6 неподвижны» верна, но неточна: свет у них тоже складывается зеркалом,
просто угол фиксирован. Для геометрии это разные случаи.

Фокусные из интринсик: A ≈ 3380 px, B ≈ 8300 px, C ≈ 18700 px. Отношения 2.46 и 5.53 против
2.5 и 5.36 для заявленных 70/28 и 150/28 — сходится в пределах пары процентов.

### Калибровка непереносима между экземплярами

Две камеры рядом, одни и те же модули:

| | разброс между экземплярами |
| --- | --- |
| фокусное `fx` | до **122 px** (C2: 18649 против 18771) |
| оптический центр `cx` | до **180 px** (C1: 2222.8 против 2041.9) |
| диапазон углов зеркала | до **1.3°** (C2: 41.38–49.15 против 39.85–46.64) |

Взять калибровку чужой камеры и применить к своей — промахнуться на сотни пикселей. Для
открытой склейки это означает: **данные каждого владельца уникальны и нужны целиком**, обобщённой
таблицы «для L16» не существует.

### Поза зеркала замкнута на кадр

`mirror_position` в `CameraModule` — это код Холла на момент съёмки, и `lri-mono --peek` его
печатает. Кадр на 150 мм, камера 1:

```text
B1 655   B2 704   B3 621   B5 647      подвижные
C1 501   C2 414   C3 197   C4 466      подвижные
B4   0   C5   0   C6   0               приклеенные
```

Модули, которые калибровка называет `GLUED`, отдают ровно ноль — независимая проверка того, что
таксономия прочитана верно. Цепочка получается полной: **код Холла из кадра → модель актуатора из
калибровки → угол зеркала → поза модуля**. Для B1 здесь: 655 при диапазоне 0…1023 и углах
36.81°…46.45° даёт около 42.98° по линейной прикидке; считать следует по квадратичной модели,
которая лежит там же.


## `hotpixel.rec` прочитан: 16 карт дефектов, по одной на модуль

Тридцать мегабайт оказались не одним файлом настроек, а **шестнадцатью полноразмерными
картами дефектов** — по одной на модуль. Читается командой `lri-mono --hotpixels`.

Устройство контейнера необычно: это обычный блок `LELR`, но протобуф-сообщение лежит **в
самом конце**, а не в начале, поэтому заголовок указывает на 29 МБ вперёд. Каждое
измерение в `HotPixelMap` описано полями `data_offset` / `data_size`, а по этому смещению
лежат ещё 20 байт своего заголовка — контрольная сумма, длина сжатых данных, ширина и
высота, — и следом zlib-поток.

Поток распаковывается в **4160 × 3120 байт, ровно один байт на фотосайт**. Это не маска
«годен/негоден», а степень: 0 у чистого пикселя, 255 у безнадёжного.

| | LFCLHMB7C0700105 | LFCLHMB7A1700334 |
| --- | --- | --- |
| условия замера | 15 мс, усиление 7.75, 37–43 °C | 15 мс, усиление 7.75, 37–43 °C |
| замеров на модуль | 1 | 1 |
| помечено (>0) | 9.9 % … 16.5 % | 10.3 % … 13.7 % |
| серьёзных (≥16) | 0.28 % … 0.72 % | 0.29 % … 0.40 % |
| безнадёжных (=255) | 17 135 … 31 593 | 16 523 … 23 648 |

Схема `hot_pixel_map.proto` допускает **серию** замеров на модуль — поля выдержки,
температуры и усиления повторяемые. Завод записал по одной точке. То есть развёртки по
экспозиции и температуре, на которую мы надеялись, в файле нет: есть один режим,
15 мс при усилении 7.75 и рабочей температуре сенсора.

### Карты не совпадают между экземплярами вообще

Прямая проверка: сколько «мёртвых» пикселей у одного и того же модуля общего у двух камер.

| Модуль | камера 1 | камера 2 | общих | это % от камеры 1 | случайное совпадение |
| --- | --- | --- | --- | --- | --- |
| A1 | 21 501 | 20 953 | 40 | 0.19 % | 0.16 % |
| A2 | 19 221 | 20 367 | 49 | 0.25 % | 0.16 % |
| B1 | 31 593 | 22 479 | 75 | 0.24 % | 0.17 % |
| C6 | 25 897 | 21 987 | 57 | 0.22 % | 0.17 % |

Совпадение **ровно на уровне случайного**, корреляция карт 0.001. Для контроля: два разных
модуля одной камеры пересекаются на тех же 0.17 %. Никакой общей структуры — ни столбцов,
ни кластеров, ни унаследованного от кремния узора — у карт нет.

Это доводит вывод о непереносимости калибровки до предела. Геометрия хотя бы близка между
экземплярами (расхождение в сотни пикселей, но модули узнаваемы). Карта дефектов **не
имеет к чужой камере никакого отношения**: у каждого из 32 сенсоров свои двадцать тысяч
битых пикселей, и позаимствовать их не у кого.

### Файл предназначен не камере, а десктопу

`hotpixel.rec` нигде не упоминается в приложении — ни в smali, ни в ресурсах. Зато галерея
выкладывает в `/sdcard/DCIM/Camera/lightcal/` ровно пять файлов, каждый со своим `.md5`:

```
asic_calib_v1.lri   calibration.lri   hotpixel.rec   uuid.txt   zoom_calib_v0.lri
```

Заводские протоколы (`colorshopfloor.txt`, `flash.json`, `colorcalibmirrorhall.json`,
`colorcamparams.rec`) туда **не попадают**. То есть выложенный набор — это в точности то,
что нужно проявщику на компьютере, и карта дефектов входит в него.

### Но правит дефекты сам ASIC, до записи плана

Первое предположение — раз файл выкладывается для десктопа, значит коррекция и делается на
десктопе — **оказалось неверным**. Проверено съёмкой по ровному полю (потолок), где нет
деталей сцены, способных замаскировать дефект.

| | камера 1, A2 | камера 2, C6 |
| --- | --- | --- |
| прошивка, приложение | 1.3.5.1, наша сборка | 1.3.4.1, **стоковое** |
| выдержка, усиление | 100 мс, 2.0× | 100 мс, 4.0× |
| клиппинг | 5.9 % | **0.00 %** |
| пикселей с ровным окружением | 2 931 867 | 1 138 898 |
| из них помечено заводом «безнадёжными» | 4 498 | 1 949 |
| **из них реально выбилось** | **0** | **0** |
| залипших в максимум во всей зоне | 0 | 0 |
| отклонение больше 100 отсчётов | 0 | 0 |

У второй камеры максимум по **всему** кадру — 375 из 1023 при 13 мегапикселях. Если бы хоть
один пиксель залип в насыщение, максимум был бы 1023. Завод считает безнадёжными 21 987 из
них; в кадре не выбился ни один.

Методика: отклонение пикселя от среднего четырёх соседей, а условия ровности и экспозиции
наложены **только на соседей** — сам пиксель ничем не ограничен, иначе залипшие в максимум
выпали бы из проверки. Проверены все четыре ориентации карты и сдвиги ±3 пикселя.

Вывод: **план в `.lri` уже вычищен от дефектов аппаратно.** Это верно и для стокового
приложения на старой прошивке, то есть правит железо, а не наша сборка.

Два следствия. Для открытой проявки — маскировать дефекты не нужно, работа уже сделана. Для
всех, кто считает `.lri` сырым, — **он не сырой**: как минимум одна интерполяция уже
применена, и битового соответствия отсчётам сенсора там нет.

Зачем тогда файл выкладывается наружу, остаётся вопросом. Возможно, Lumen использует его не
для коррекции, а чтобы знать, какие пиксели восстановлены, и не доверять им при склейке.

### Кадр можно привязать к камере: `lri-mono --fingerprint`

В `.lri` **нет ни серийника, ни uuid** — поиск по обоим файлам из архива не находит ничего.
Кадр, отделённый от камеры, безымянен. Но заводская геометрия едет в каждом кадре, а она
поштучная, и этого достаточно:

```
L16_00087.lri   отпечаток 70a9606d58c6035a   среднее расхождение с камерой 1: 0.000 px
                                             с камерой 2:                   159.806 px
```

Оба архивных кадра совпали с камерой 1 **бит в бит**, при расхождении между самими камерами
в 160 px. Ошибиться тут не на чем.

Практический смысл: архив осиротевших `.lri` раскладывается по камерам и связывается с
нужным набором калибровки, а владелец может проверить, его ли это кадр. Для сообщества,
которое годами копило снимки с разных экземпляров, это единственный способ восстановить
связь — потому что сама камера её не записывает.

## Что дальше
- [x] `hotpixel.rec` — прочитан, см. выше.
- [x] Применяет ли ASIC карту дефектов до записи плана — да, см. выше. Осталась узкая
      щель: горячие пиксели, видимые только на длинной выдержке в темноте, проверкой по
      ровному освещённому полю не покрываются. Закрывается кадром с крышкой на объективе.
- [ ] `colorcamparams.rec` и `crosstalkcamparams.rec` — бинарные параметры, формат неизвестен.
- [ ] Проверить, сохранились ли `shopfloor`-файлы на других ранних экземплярах: если да, это
      воспроизводимый источник, а не случайность одной камеры.
