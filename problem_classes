# Problem classes tracker
# Status: open | fixed | smali-only

openlight/co/camera/haptic/PlayHaptic.java:
	open — depends on Immersion native lib; null-safe CameraManager added

openlight/co/lib/exif/ExifInterface.java:
	fixed — AOSP port; HEIC/AVIF unsupported by design

openlight/co/camera/view/setting/SettingAdapter.java:
	fixed — ManagePresetsActivity class name

openlight/co/camera/view/hud/HudLayout.java:
	fixed — switch map inlined from anonymous $3

openlight/co/camera/utils/LocationManager.java:
	fixed — scheduleSave NPE, NMEA bounds, PendingIntent flags

openlight/co/camera/CameraActivity.java:
	fixed — reloads FeatureManager before fragment init

openlight/co/camera/CameraInfo.java:
	fixed — null guards; emulator flags camera.require_raw / camera.relax_yuv_size

openlight/co/camera/managers/CameraManager.java:
	fixed — readyForFocus, listener NPEs, fpsRange null, openCamera guards

openlight/co/camera/managers/video/VideoManager.java:
	fixed — listener and camera device null checks (phase 0)

openlight/co/touchstrip/TouchStrip.java:
	fixed — synchronized get() (phase 0)

openlight/co/camera/utils/Util.java:
	fixed — shared_pref_update migration; removed dead catch (phase 0)

openlight/co/camera/managers/capture/CaptureManager.java:
	fixed — migrated from smali (phase 2); Provider method refs

openlight/co/camera/managers/capture/CaptureBurstManager.java:
	fixed — migrated from smali (phase 2); enum switch inlined

openlight/co/camera/managers/focus/FocusManager.java:
	fixed — migrated from smali (phase 2)

openlight/co/camera/managers/focus/SmartAFTriggerMgr.java:
	fixed — migrated from smali (phase 2); KeyAction switch inlined

# Still smali-only on critical path — see scripts/smali-debt.txt