# Открытые вопросы

Живой список, к которому возвращаемся. Каждый пункт — чем закрывается, а не только что
непонятно. Статус на 04.08.2026.

Подробности по темам: [hidden-features.md](hidden-features.md) — инвентарь выключенного,
[monochrome.md](monochrome.md) — монохромные модули и развилка по архитектуре,
[questions-for-light.md](questions-for-light.md) — подсписок «только очевидец»: что имеет
смысл спрашивать у бывшей команды Light, а что мы закрываем сами.

## Ждут решения

- [ ] **Куда девать монохром.** Четыре варианта в `monochrome.md` §5: отдельное приложение,
      ортогональный тумблер, дотянуть режимом, рендер через `MonoFusion` галереи. Пока не
      выбрано, доделывать режим смысла нет.
- [ ] **Что коммитить.** Несобрано в гит: режим `mono` с миграцией `CameraMode`, `lri-mono`,
      ускорение тач-стрипа, переезд сабмодуля на форк, документы. Перед коммитом убрать
      временный лог `[ZOOMFEEL]` из `TouchStrip`.

## Видео: что вскрылось при разборе тестов

- [ ] **Темп 40 к/с там, где API обещает 30.** `L16_00116.mp4`, 2688×1512: медиана интервала
      24.3 мс, средний темп 40.0 к/с за 4.3 с. Camera2 при этом объявляет
      `availableMinFrameDurations` = 33.3 мс на всех разрешениях. Значит конвейер ASIC не
      ограничен тем, что видит фреймворк, и вопрос `co.light.burst_fps` надо пересматривать.
- [ ] **4K UHD достижим, но разваливается.** `L16_00145.mp4`, 3840×2160: контейнер 65.4 с,
      видеодорожка обрывается на 25.1 с, звук пишется до конца. Средний темп 21.4 к/с,
      первые 60 кадров идут по 29 мс (~34 к/с), последние — по 115 мс (~9 к/с). Причина не
      установлена: термика, пропускная способность записи или кодер. Проверять на холодную,
      на батарее, с разными битрейтами.
- [ ] **Нарезка на 4 ГБ.** `L16_00103.mp4` (3.77 ГБ) и `L16_00103_002.mp4` — автоматическое
      деление сработало. Не проверено, стыкуются ли куски без потери кадров на склейке.
- [ ] **Краш профиля «high».** Файл обрывается на 3227 байтах, воспроизводится. Падает
      штатное приложение, поэтому стека нет. Нужен чистый лог с момента запуска.

## Матрицы: перепись всех шестнадцати

- [x] **Второй экземпляр подтверждает монохромные модули.** 17.08.2026 на шину встала вторая
      камера, LFCLHMB7A1700334 с прошивкой `00WW_1_341` (у первой `00WW_1_351`). Её
      единственный снимок: A2 — `cfa none`, потайловый тест **1.5%** против **63.3%** у A1.
      Значит находка не про конкретный аппарат и не про версию ПО. Проверка заняла минуту:
      `ANDROID_SERIAL=<серийник> lri-mono --peek A2 --device …` — тест сетки теперь печатается
      прямо при точечном чтении, полный файл тянуть не нужно.


Сделана `lri-mono --census` по двум кадрам — широкому (28 мм) и телевику (103 мм),
вместе они покрывают все id. Что видно:

- [x] **Третьего типа матрицы нет.** Четырнадцать цветных AR1335 и два панхроматических.
      Заводские отношения каналов гуляют в пределах 5% (`rg` 0.472…0.516, `bg` 0.650…0.711) —
      это экземплярный допуск, а не другие фильтры.
- [x] **Колориметрия у каждого модуля своя и постоянна.** Три осветителя — D65, A, F11.
      B2 в обоих файлах даёт ровно `rg 0.491 / bg 0.667`, B4 — `0.516 / 0.711`: значения
      заводские, а не пересчитываемые на кадр. У монохромных профилей нет вовсе.
- [x] **Группы работают не так, как я записал раньше.** В кадре 28 мм участвуют пять A и
      пять B, в кадре 103 мм — пять B и шесть C. То есть A/B/C — это фокусные самих модулей,
      а не разбиение по тому, кто снимает какой кадр; B участвует в обоих.
- [x] **Уровень сигнала между модулями: не экспозиция.** `CameraModule` несёт
      `sensor_exposure` и `sensor_analog_gain` на каждый модуль, `lri-mono --census` их теперь
      читает. Они почти одинаковые: 30.42 мс у всех, усиление 6.5× у всех, кроме опорной A1 с
      3.25×. После нормировки внутри группы 28 мм цветные дают 0.51…0.67, монохромный A2 —
      1.18, то есть **ровно вдвое, один стоп**. Прежний пятикратный разброс весь пришёлся на
      группу B: это 70 мм, каждый модуль кадрирует свой кусок сцены. Остаётся стенд —
      но уже не для этого вопроса, а для виньетирования и поля зрения.

      Со стенда — плоское поле:
      - ровная матовая стена или большой лист, равномерно освещённый, с расстояния 2–3 м,
        чтобы в кадр самого широкого модуля не попадало ничего кроме неё;
      - лёгкая расфокусировка, чтобы фактура стены не мешала;
      - режим `manual` с зафиксированными ISO и выдержкой, чтобы запрос ко всем модулям был
        одинаковым; если уровни всё равно разъедутся — значит экспозиция раздаётся модулям
        независимо, и это ответ на главный вопрос;
      - три уровня освещённости, отличающиеся примерно вдвое, чтобы проверить, ходят ли
        уровни модулей синхронно;
      - этот же кадр закрывает виньетирование и падение резкости к краям, а на широком —
        вопрос про обрезку поля зрения.

      Со стенда — мира: снять с известного расстояния, посчитать реальное поле зрения каждого
      модуля и сравнить с заявленным ЭФР. Это прямой замер того самого кроп-вопроса: 16.6 мм
      против выдаваемых 28.
- [x] **Выигрыш монохромного модуля** по двум кадрам: A2 даёт 233 против среднего 108 у
      цветных (1.1 стопа), C6 — 206 против 118 (0.8 стопа).

## Движок Light вызываем с десктопа

Внутри `Lumen.app/Contents/Frameworks/` лежит **`libcp.dylib`** — та же библиотека
вычислительной фотографии, что и в галерее на камере, но собранная под macOS x86_64 и,
в отличие от андроидной, с нормальной таблицей символов. Поверх неё публичный C++ API
`CIAPI`:

```text
CIAPI::Renderer::Create(RendererProfile)
CIAPI::RendererBase::setInputDataStream(const void*, unsigned long)   ← сюда байты .lri
CIAPI::RendererBase::setProperty / getProperty (ParamInt, ParamFloat, ParamString, …)
CIAPI::Renderer::render(int, ROI, RenderType, bool)
CIAPI::Renderer::outputBuffer()
CIAPI::Transform  — кроп, поворот, соотношение сторон
CIAPI::StateFileEditor, CIAPI::DepthEditor  — состояние снимка и правка глубины
```

Имена свойств лежат строками: `exposure`, `contrast`, `clarity`, `saturation`,
`sharpening`, `vibrance`. `MonoFusion` и `SensorType::Panchromatic` — там же.

Проверено: библиотека **грузится на Apple Silicon под Rosetta** (рядом нужны
`libceres.dylib` и `liblricompression.dylib` из того же каталога), символы
`GetVersion`, `Renderer::Create`, `setInputDataStream`, `outputBuffer` разрешаются.

- [x] **Харнесс написан — Grok, 05.08.2026.** `.lri` → картинка родным движком, сверено с
      кэшем Lumen (MAE≈11, r≈0.98). Рецепт и путь — `GROK.RESEARCHES.md` § A1. Проверки
      лицензии не встретилось; профиль рендера задаётся в `Create(...)`.
- [x] **Экспозиция и усиление на модуль** оказались доступны и без движка — прямо из
      контейнера, `lri-mono --census`.
- Дальнейшая десктопная работа живёт в отдельном репозитории **luminat**, не здесь.
  Вариант Г из `monochrome.md` закрыт с той стороны.

## Закрываются короткой правкой

- [x] **Хук моно на всех путях — сделано.** Оба прямых места в `ModeReqMgr` переведены на наш
      хелпер. Результат: **ASIC слушается `CONTROL_EFFECT_MODE`** — живой вид в монохроме
      действительно чёрно-белый, и снап фокусного тоже заработал на превью (HUD показывает 28
      вместо 35). А вот сохраняемый прокси-JPEG остался цветным: эффект живёт только в потоке
      превью, снимок собирает фьюжн заново из данных модулей.
- [x] **Обесцветить прокси при сохранении — сделано.** 196 мс на кадр, метаданные
      переносятся, веса панхроматические (полосы поровну), не яркостные.
- [x] **HUD в mono raw честен**: `13 mp·A2` вместо `52+ mp`, модуль меняется на `C6` при 150 мм.
- [~] ~~**Обесцветить прокси при сохранении.**~~ Единственное, что в монохромном режиме осталось
      цветным. Полтора мегапикселя, `ImageReaderManager` уже на Java — декодировать, применить
      яркость, упаковать обратно.
- [ ] **Поправка −1 EV в монохроме — но не всегда.** В кадре с окном в сцене панхроматический
      план клиппился на 9%, а на ровной стене 04.08 — на 0.12% при уровне 298. Значит поправка
      нужна по сюжету, а не постоянная; либо мерить клиппинг и подстраивать.
- [ ] **`lri-mono --trails`.** Вытянутость звёзд по вторым моментам — предел выдержки
      измеряется по кадру, а не по формуле.
- [ ] **Медиа-коды в `HardKeyManager`.** Сейчас разбираются `0x18` VOLUME_UP, `0x1b` CAMERA,
      `0x50` FOCUS. Пульты в «iOS-режиме» шлют медиа-клавиши и потому не работают.
- [ ] **Рулетка на две позиции.** Упирается в миграцию `ZoomManager`: его арифметика зума
      рассчитана на четыре прайма.

## Закрываются только съёмкой

- [~] **Астро-потолки: в интерфейсе пробиты, в железе нет.** Добавлены ступени 20, 25 и
      **29.98 с** (ровно тот максимум, что объявляет сенсор, а не круглые 30, которые он мог бы
      отвергнуть) и ISO 4000…12800. Все четыре таблицы сошлись: `exposureTimes` 56,
      `ShutterSpeedValues` 56, `shutter_value` 56, `SensitivityValues` 21, `iso_value` 21.
      Приложение приняло оба предельных значения через свои преобразования — в логе
      `Updated ISO to 12800` и `Updated Exposure Time to 29981853000`, бинарный поиск в
      `getCurrentIso` не сломался, падений нет.

      **HAL принял.** Кадр `L16_00185.lri`, снятый рукой в ручном режиме с выкрученным колесом,
      записан с выдержкой **19 450 мс** — всеми десятью модулями, значение прочитано из самого
      контейнера. Прежний потолок 15 с пробит, то есть вендорные ключи стеной не были: предел
      ставила себе сама программа своей таблицей.

      **Поправка от 17.08.2026: потолок не пробит, пробит только индикатор.** Колесо
      доведено до самой верхней ступени — на экране честные `29.98 s`, — и кадр
      `L16_00231.lri` всё равно записан с **19450.065 мс**, теми же десятью модулями.
      Прежняя догадка «просто не докручено» неверна: 19.45 с — жёсткий предел ниже
      интерфейса, а не квантование ступени 20 с.

      То же с ISO. На экране `12800`, а в кадре `L16_00233.lri` аналоговое усиление
      **7.75×** при цифровом 1.00 — ровно столько же, сколько даёт ISO 800. При ISO 200
      усиление 2.00×, то есть до 800 оно следует за настройкой и там останавливается.
      Похоже, 7.75× — предел самой матрицы AR1335, а всё выше приложение обязано добирать
      цифрой уже после съёмки; в план это не попадает.

      Что это значит для астро: **реальные потолки — 19.45 с и усиление 7.75× (ISO 800)**.
      Расширенные таблицы дают выбрать больше, но в данные это не проходит. Дальше можно
      только складывать кадры — тем более что складывать L16 умеет и в пространстве.

      Старое уточнение по
      вендорным ключам: они **не стена от HAL** — `setIsoPriority(min, max)` и
      `setShutterPriority(min, max)` в `lightsdk` выставляет само приложение, то есть
      расширенные значения уедут в `co.light.iso_range_min/max` и `shutter_range_min/max`
      автоматически. Вопрос сводится к одному: примет ли их HAL.
- [ ] **Кроп широкого модуля.** RAW с 2.8 мм: есть ли данные за границей выдаваемого кадра.
- [x] **C6 на 150 мм — подтверждён.** Снято 04.08 в режиме `mono` на 150 мм: файл 179 МБ,
      одиннадцать модулей, C6 на месте, план вытащен `--peek`. Но на дистанции около метра он
      сильно расфокусирован и смотрит заметно левее видоискателя — телемодуль не дотягивает до
      своей минимальной дистанции, и параллакс на таком расстоянии огромен. Для астро неважно,
      для настольных проверок C6 непригоден. Усиление ему выдаётся 7.75× против 3.62× у A2 при
      той же выдержке — потери в сложенной оптике.
- [x] **Термика на длинных сериях — измеряется из самого кадра.** `CameraModule` несёт
      `sensor_temparature`, `lri-mono --peek` его теперь печатает. Четыре подряд по 19.45 с при
      +28° в комнате: **39° → 41° → 47° → 47°**, то есть плюс восемь градусов за четыре кадра и
      выход на полку. Тёмновой ток удваивается примерно на каждые 6–8°, значит к четвёртому
      кадру фон шумит вдвое сильнее первого. Для серий отсюда следует: либо паузы, либо дарки
      при той же температуре, что и кадры.
- [ ] Осталось померить то же самое на зарядке и на более длинной серии — полка на 47° может
      оказаться не полкой, а началом подъёма.
- [ ] **`burst_fps`** — замер по таймстемпам, с учётом находки про 40 к/с выше.

## JPEG на камере — это прокси, а не снимок

- [x] **Сохраняемый JPEG всего 1440×1080.** Проверено на обоих сегодняшних кадрах,
      `L16_00181.jpg` и `L16_00182.jpg`: 1.5 Мп при заявленных в HUD 13 и 52. То есть камера
      кладёт рядом с `.lri` лишь превью, а полноразмерный снимок собирается позже — галереей
      или Lumen из контейнера.

      По мануалу уровней три: превью при съёмке, **13 Мп сборка на камере** и **52 Мп в
      Lumen**. То есть снимок рождается не в момент спуска, а при сборке из контейнера.

      Почему именно так — выяснено (Grok, `GROK.RESEARCHES.md` § D): движок один и тот же,
      разница в **`RendererProfile`**. Мобильный `Create(1/2)` даёт 13 Мп, десктопный
      `Create(3)` — полотно 10432×7824. То есть 13 Мп на устройстве это продуктовый и
      тепловой предел, а не «модули больше не пишут»; в мобильном профиле super-res просто
      отключён строкой `This profile doesn't support super-resolution`.

      Это меняет формулировку претензии к монохромному режиму: он отдаёт не «цветной снимок
      вместо чёрно-белого», а **цветное превью**, тогда как настоящий кадр в любом случае
      рождается из `.lri`. Значит путь через `--peek` не обходной, а основной, и вопрос
      «почему JPEG цветной» частично снимается сам.

      Симметрия, которую стоит держать в голове: план монохромного модуля — 4160×3120, те же
      13 Мп, что собирает камера. Только у сборки они получены склейкой и дебайеризацией, а у
      A2 напрямую, без единой интерполяции.

## Падения теперь оставляют след

- [x] **Свой обработчик исключений — сделано.** Light отдаёт трассировку в HockeyApp,
      который мёртв с 2019 года, и после неудачной отправки удаляет её; в логе остаётся
      только `System.exit(2)`. Теперь трассировка дописывается в `/sdcard/openlight-crash.log`
      и лишь затем передаётся их обработчику — поведение приложения не меняется.

      Две ловушки по дороге, обе стоили времени: HockeyApp регистрируется **не** синхронно из
      `onResume`, а через собственный хендлер, и приземляется поверх нас — поэтому захват
      перепроверяется через 5 секунд. И **исключения с главного потока куда-то деваются**:
      брошенное в `postDelayed` не роняет приложение и не доходит ни до одного обработчика.
      Проверять сеть нужно с фонового потока; флаг `crash.selftest` в `features.prop` делает
      это одной перезагрузкой.

## Известные поломки

- [x] **Тест-интент `mode_test` больше не роняет.** Перестал воспроизводиться после перевода
      обоих мест `ModeReqMgr` на наш хелпер: превью само снапит фокусное на монохромную
      ступень, и рассинхронизации индекса зума с урезанным списком праймов больше не возникает.
      Проверено на 05.08 повторно, в том числе с возвратом на 35 мм перед переключением.
      Прежняя запись:
- [~] ~~**Тест-интент `mode_test` роняет приложение.**~~ Переключение режима через
      `am broadcast --es mode_test MONO` приводит к `System.exit(2)` от крэш-хендлера Light,
      причём и в auto, и в mono. Через рулетку в интерфейсе тот же переход проходит нормально,
      `focal_length_test` тоже не роняет. Трассировки нет: хендлер шлёт её в HockeyApp, а не в
      логкат. Подозрение на то, что интент меняет режим мимо штатного перехода, и урезанный в
      монохроме список праймов расходится с текущим индексом зума.

## Ждут проверки на столе

- [ ] **Тач-стрип после правки пачек.** Медленное движение было хорошим, быстрое прыгало
      пачками; выдача разложена по кадрам, нужен повторный тест.
- [x] **Гимбал DJI RS4 Mini — работает.** Подключается как Bluetooth-клавиатура
      `/dev/input/event17` («DJI RS4 MINI-061B672», второй бонд) и шлёт единственную клавишу
      `KEY_VOLUMEUP`, HID usage `000c00e9`, со **всех** кнопок — колесо и джойстик наружу не
      выходят. Наша сборка её уже понимает: код 24 → `FULL_KEY` → спуск, проверено по логу от
      `onKeyDown` до `MSG_TRIGGER_CAPTURE_TO_HAL` и по трём новым файлам. Кабель проверять
      незачем.
- [ ] **Уплотнить единственную клавишу гимбала — но словарь беднее, чем кажется.** По
      документации DJI гимбал различает полунажатие (автофокус), короткое (старт-стоп видео) и
      удержание (фото), однако это работает **только с поддерживаемыми камерами**, где он
      говорит их протоколом. Нам достаётся обобщённый режим и один щелчок: в захвате все шесть
      событий короткие, между DOWN и UP от 0.14 до 9.6 мс, независимо от того, что нажимали.
      Значит различать длительность на стороне камеры бесполезно — до нас всегда приходит
      короткое. Остаётся счёт тапов: два быстрых нажатия видны как два щелчка. Переднее колесо
      и джойстик наружу не выходят вовсе.
- [ ] **`FULL_KEY` в видеорежиме** — стартует ли запись или делает кадр.
- [ ] **Индикатор на кнопке спуска.** RGB с аппаратным миганием, приложением не занят. Идея:
      цвет уведомления через NotificationManager, не проверено.

## Управление гимбалом: ветка упирается в один разъём

Спуск с гимбала уже работает по HID, но это весь канал. Всё, что богаче — движение по осям,
запись и воспроизведение траекторий — существует, и протокол **опубликован самой DJI**:
«DJI R SDK protocol v2.2». Три независимых открытых проекта его используют, и все три идут
**проводом в порт RSA по CAN**, ни один не трогает Bluetooth:

- [ConstantRobotics/DJIR_SDK](https://github.com/ConstantRobotics/DJIR_SDK) — RS 2, CAN через
  USBCAN-II: `move_to`, `set_speed`, `get_current_position`, все три оси, шаг от 100 мс.
- [rileyharmon/DJI-Ronin-RS2-Log-and-Replay](https://github.com/rileyharmon/DJI-Ronin-RS2-Log-and-Replay)
  — RS2 и RS3 Pro, CAN через CANable: запись и воспроизведение движений плюс мотор фокуса.
- [MrColoo/DJI-Ronin-Remote-Controller-ESP32](https://github.com/MrColoo/DJI-Ronin-Remote-Controller-ESP32-ESP8266)
  — Ronin SC, ESP32 в порт SBUS, только панорама и наклон.

- [ ] **Решающая проверка не программная: есть ли на RS4 Mini порт RSA или CAN.** По
      документации DJI у Mini упоминается только NATO, RSA описан для полноразмерного RS 4.
      Смотреть на корпус.
- [ ] Если порт есть — вырисовывается моушен-таймлапс без всякой интеграции: компьютер по CAN
      проигрывает записанную траекторию, камера снимает сама, спуск уже идёт по HID-клавише.
      Синхронизация нужна только по времени. С камеры напрямую не выйдет: её USB — порт
      устройства, не хоста, понадобится посредник вроде ESP32.
- Мобильный SDK DJI тут не поможет: он про дроны (Mavic 3 Enterprise, M30, M300 RTK),
  RS-серии в поддерживаемых нет, плюс требует активации ключом.

## Заводская калибровка (см. `calibration.md`)

- [x] **Зеркала — на восьми модулях, и известно каких.** B1, B2, B3, B5 и C1–C4; B4, C5, C6 и вся
      группа A неподвижны. Из `colorcalibmirrorhall.json` на ранней камере: код Холла и две точки
      «показание → угол» на каждый модуль. Это прямой вход в задачу позы зеркала.
- [x] **Вспышка двухцветная** — CoolWhite 5285 K и WarmWhite 2194 K, замер поштучный.
- [x] **Связать `mirror_position` из `.lri` с таблицей Холла** — цепочка замкнута: код Холла из
      кадра → модель актуатора → угол зеркала. Приклеенные модули отдают ровно ноль.
- [x] **Разобрать `calibration.lri` протобуфом** и сравнить две камеры — сделано, `--calib`.
      Поштучно всё: фокусные расходятся до 122 px, оптические центры до 180 px.
- [x] **`hotpixel.rec`** — шестнадцать полноразмерных карт дефектов, по одной на модуль,
      байт на фотосайт, замер один: 15 мс при усилении 7.75. Карты двух камер пересекаются
      ровно на уровне случайного — заимствовать чужую нельзя вообще.
- [x] **Кадр привязывается к камере — и проверяется.** Кадр называет свою камеру сам:
      `device_unique_id_low`/`high` в `LightHeader`, склейка = `uuid.txt` раздела
      `/lightcal`, совпадение посимвольное на обеих камерах. (Раньше я написал, что uuid
      в кадре нет, — ошибка была в способе поиска: он лежит varint'ами, а я искал строку.)
      Вдобавок `lri-mono --fingerprint` считает хэш заводской геометрии — независимая
      проверка, что набор калибровки действительно от этой камеры.
- [ ] **Применяет ли ASIC карту дефектов до записи плана.** На дневном кадре (усиление 1.0,
      5 мс) помеченные пиксели неотличимы от чистых, но при таком усилении их и не должно быть
      видно. Закрывается одним снимком: крышка на объектив, высокое ISO, несколько секунд.
- [ ] **`colorcamparams.rec` / `crosstalkcamparams.rec`** — формат неизвестен.

## Загадки без ответа

- [ ] **Зачем монохромные модули в крайних группах, а в средней нет.** В бинарниках
      объяснения нет.
- [~] **Участвует ли A2 в итоговой склейке — наполовину закрыто.** Из строк `libcp` (Grok,
      § R10): mono **не** в super-res и **никогда** не опорный кадр, но имеет свой путь
      `MonoFusion` и, вероятно, кормит глубину и яркость. Осталась живая проверка пальцем:
      два кадра 28 мм с одной точки, во втором A2 закрыт целиком; сравнить детали и шум
      склейки, план A2 во втором должен быть тёмным. Полный протокол — § D4 у Grok.
- [ ] **Причина обрезки широкого угла** — виньетирование, пересечение полей зрения при фьюжне
      или калибровка только по центру.
- [ ] **Четыре недостроенные строки настроек** — `aspect_ratio_setting`, `video_format`,
      `dof_setting`, `device_cam_finder_timeout_setting`: списки вариантов пустые.
- [ ] **Редактирование на камере** — четыре RenderScript-ядра без вызывающих, байткод утрачен.
- [ ] **Стекинг модулей одного фокусного** — мешает разная ориентация и зеркальные тракты.

## Закрыто

- [x] **Монохромные модули найдены** — A2 (28 мм) и C6 (150 мм), пять независимых
      доказательств, выложено в форк архива.
- [x] **Обновление Android** — не имеет смысла: Treble нет, вендорный слой собран под 6.0,
      на новой версии камера умрёт целиком, а выигрыша для наших задач нет.
- [x] **Почему тач-стрип не чувствовал скорость** — `onScroll` выбрасывал колбэки, где палец
      прошёл больше 150 единиц, то есть чем быстрее движение, тем большая часть терялась.
